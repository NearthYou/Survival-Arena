add_library(
    dxa_protocol
    STATIC
        src/ByteCodec.cpp
        src/TcpFrame.cpp
)
target_include_directories(
    dxa_protocol
    PUBLIC
        ${CMAKE_CURRENT_SOURCE_DIR}/include
)
dxa_enable_project_warnings(dxa_protocol)
